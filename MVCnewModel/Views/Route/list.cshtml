@using MVCnewModel.Models
@model ICostRepository
@addTagHelper *,Microsoft.AspNetCore.Mvc.TagHelpers


<!DOCTYPE html>

<html>
<head>
    <title>title</title>
</head>
<body>
<div>
    @{
        var  costModel = @Model as StaticCostRepository;
    }
    @foreach (var cost in costModel.costs)
    {
        // <partial name="costPartialView" model="cost"/>
        // 
        // await Html.PartialAsync("costPartialView", cost);
        await Html.RenderPartialAsync(partialViewName: "costPartialView", model: cost);
    }
</div>
</body>
</html>